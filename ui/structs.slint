export struct Header {
    key: string,
    value: string,
}

export struct Message {
    id: string,
    header: [Header],
    body: string,
}

export struct Box {
    query: string,
    messages: [Message],
}

